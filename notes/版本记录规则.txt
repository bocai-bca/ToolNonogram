发行版版本记法：
采用字符串记录，按三段数字点记法记录，例如1.8.2。
当填写版本的地方要求使用四段数字时，可使最后一位数字始终为0。
当上传非发行版时，可在字符串版本号的末尾加一个E表示Edited，如1.9.0E或1.9.0-E。

一般git上传版本记法：
采用无符号整数记录，以0为起始，如果0被仓库初始上传占据，那就以1为起始。
使用无符号整数记法且当需要以字符串呈现时，在字符串首部加一个v，例如v32768。
上传可以不每次都进行版本记录，但是每次版本记录都代表一个上传单位。